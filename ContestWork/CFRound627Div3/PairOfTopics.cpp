#include <iostream>
using namespace std;

int main () {
    int topics; cin >> topics;
    int c_good = 0;
    int teacher[topics]; int students[topics];
    for (int i = 0; i < topics; i++) {
        cin >> teacher[i];
    }
    for (int i =0; i < topics; i++) {
        cin >> students[i];
    }
    //Do this with binary search instead of how you wanted to.
    
}