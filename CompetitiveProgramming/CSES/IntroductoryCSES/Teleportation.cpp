#include <iostream>
using namespace std;

int main() {
    int distance[4];
    for (int x = 0; x<4; x++) {
        cin >> distance[x];
    }
    //check whether the teleporter is closer to the end or whether just taking the tractor is faster
}